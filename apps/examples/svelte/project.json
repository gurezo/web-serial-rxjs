{
  "name": "svelte",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/examples/svelte/src",
  "projectType": "application",
  "tags": ["type:app", "scope:examples"],
  "targets": {
    "build": {
      "executor": "@nx/vite:build",
      "outputs": ["{options.outputPath}"],
      "defaultConfiguration": "production",
      "options": {
        "outputPath": "dist/apps/examples/svelte",
        "configFile": "apps/examples/svelte/vite.config.ts",
        "skipTypeCheck": true
      },
      "configurations": {
        "development": {
          "mode": "development"
        },
        "production": {
          "mode": "production"
        }
      }
    },
    "serve": {
      "executor": "@nx/vite:dev-server",
      "defaultConfiguration": "development",
      "options": {
        "buildTarget": "svelte:build"
      },
      "configurations": {
        "development": {
          "buildTarget": "svelte:build:development",
          "hmr": true
        },
        "production": {
          "buildTarget": "svelte:build:production",
          "hmr": false
        }
      }
    },
    "preview": {
      "executor": "@nx/vite:preview-server",
      "defaultConfiguration": "production",
      "options": {
        "buildTarget": "svelte:build:production"
      },
      "configurations": {
        "production": {
          "buildTarget": "svelte:build:production"
        }
      }
    },
    "test": {
      "executor": "@nx/vitest:test",
      "outputs": ["{workspaceRoot}/coverage/apps/examples/svelte"],
      "options": {
        "passWithNoTests": true,
        "reportsDirectory": "../../../coverage/apps/examples/svelte"
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint",
      "outputs": ["{options.outputFile}"]
    }
  }
}
