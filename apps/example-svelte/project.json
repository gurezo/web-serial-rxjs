{
  "name": "example-svelte",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/example-svelte/src",
  "projectType": "application",
  "tags": ["type:app", "scope:examples"],
  "targets": {
    "build": {
      "executor": "@nx/vite:build",
      "outputs": ["{options.outputPath}"],
      "defaultConfiguration": "production",
      "options": {
        "outputPath": "dist/apps/example-svelte",
        "configFile": "apps/example-svelte/vite.config.ts",
        "skipTypeCheck": true
      },
      "configurations": {
        "development": {
          "mode": "development"
        },
        "production": {
          "mode": "production"
        }
      }
    },
    "serve": {
      "executor": "@nx/vite:dev-server",
      "defaultConfiguration": "development",
      "options": {
        "buildTarget": "example-svelte:build"
      },
      "configurations": {
        "development": {
          "buildTarget": "example-svelte:build:development",
          "hmr": true
        },
        "production": {
          "buildTarget": "example-svelte:build:production",
          "hmr": false
        }
      }
    },
    "preview": {
      "executor": "@nx/vite:preview-server",
      "defaultConfiguration": "production",
      "options": {
        "buildTarget": "example-svelte:build:production"
      },
      "configurations": {
        "production": {
          "buildTarget": "example-svelte:build:production"
        }
      }
    },
    "test": {
      "executor": "@nx/vitest:test",
      "outputs": ["{workspaceRoot}/coverage/apps/example-svelte"],
      "options": {
        "passWithNoTests": true,
        "reportsDirectory": "../../../coverage/apps/example-svelte"
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint",
      "outputs": ["{options.outputFile}"]
    }
  }
}
