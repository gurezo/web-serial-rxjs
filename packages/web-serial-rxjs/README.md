# web-serial-rxjs

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build web-serial-rxjs` to build the library.

## Running unit tests

Run `nx test web-serial-rxjs` to execute the unit tests via [Vitest](https://vitest.dev/).
